import 'package:flutter/material.dart';

class ExampleOfHttpTimeoutPage_Body extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: <Widget>[
        // TODO
      ],
    );
  }
}
