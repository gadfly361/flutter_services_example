import 'package:flutter/material.dart';

class TemplatePage_Body extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: <Widget>[
        // TODO
      ],
    );
  }
}
